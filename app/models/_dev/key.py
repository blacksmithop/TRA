from pydantic import BaseModel
from typing import List, Optional, Union, Dict

# Note: Due to truncation, no Key schemas are available in the provided document.
# If additional Key schemas exist in the full spec, add them here.